<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Rentobuy" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="actionBarTheme">@style/AppTheme.PopupOverlay</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.Material3.ActionBar">
        <!-- Customize overlay attributes here -->
        <item name="android:background">@color/primary</item>
        <!-- Add any other customizations -->
    </style>

    <style name="Theme.Rentobuy" parent="Base.Theme.Rentobuy" />
</resources>